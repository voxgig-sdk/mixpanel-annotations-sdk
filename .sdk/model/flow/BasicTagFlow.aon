# BasicTagFlow

main: kit: flow: BasicTagFlow:
{
  entity: "tag",
  kind: "basic",
  name: "BasicTagFlow",
  step: [
    {
      input: {
        ref: "tag_ref01"
      }

      match: {
        project_id: "project01"
      }

      op: "create"
    }
  ],
  "key$": "BasicTagFlow"
}